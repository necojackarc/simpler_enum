require 'spec_helper'

describe SimpleEnum do
  it 'has a version number' do
    expect(SimpleEnum::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
